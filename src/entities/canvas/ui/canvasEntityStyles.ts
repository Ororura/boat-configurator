import { StyleSheet } from "react-native";

const canvasEntityStyles = StyleSheet.create({
  image: {
    borderRadius: 15,
    height: 250,
    objectFit: "cover",
    width: 400,
  },
});

export { canvasEntityStyles };
